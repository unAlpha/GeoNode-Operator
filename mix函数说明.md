# Mix 函数使用说明

## 功能简介

`mix` 函数已成功添加到几何节点数学表达式插件中。这是一个三参数函数，用于在两个值之间进行线性插值（也称为 lerp）。

## 语法

```
mix(a, b, factor)
```

### 参数说明

- `a` - 第一个值（当 factor = 0 时的值）
- `b` - 第二个值（当 factor = 1 时的值）
- `factor` - 混合因子（0 到 1 之间，0 返回 a，1 返回 b，0.5 返回中间值）

## 数学原理

```
result = a * (1 - factor) + b * factor
```

## 使用示例

### 示例 1：简单插值
```
mix(0, 10, 0.5)
```
结果：5（0 和 10 的中点）

### 示例 2：变量插值
```
mix(min_val, max_val, t)
```
在 `min_val` 和 `max_val` 之间根据 `t` 进行插值

### 示例 3：复杂表达式
```
mix(sin(x), cos(x), 0.5)
```
混合正弦和余弦函数

### 示例 4：动画关键帧
```
mix(start_pos, end_pos, clamp(time, 0, 1))
```
从起始位置到结束位置的平滑过渡

## 在 Blender 中的实现

插件会自动创建一系列数学节点来实现 mix 函数的数学公式：

```
result = a * (1 - factor) + b * factor
```

具体节点链如下：
1. **Subtract 节点**：计算 `1 - factor`
2. **Multiply 节点 #1**：计算 `a * (1 - factor)`
3. **Multiply 节点 #2**：计算 `b * factor`
4. **Add 节点**：将两项相加得到最终结果

这种实现方式使用基本的数学节点，**确保在所有 Blender 版本中都能正常工作**，不依赖于特定版本引入的 Mix 节点。

## 快速开始

1. 打开 Blender 几何节点编辑器
2. 在侧边栏（按 N 键）找到 "Math" 标签
3. 点击"快速示例"查看 `mix(a, b, t)` 示例
4. 点击"线性插值"按钮插入示例表达式
5. 点击"创建节点组"生成节点
